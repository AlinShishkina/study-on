{% extends 'base.html.twig' %}

{% block title %}Редактировать урок{% endblock %}

{% block body %}
    <h1>Редактировать урок</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Сохранить'}) }}

    {{ include('lesson/_delete_form.html.twig') }}
    
    <a class="btn btn-outline-secondary mt-3" href="{{ path('app_course_show', {'id': lesson.course.id}) }}">Вернуться к списку уроков</a>

{% endblock %}